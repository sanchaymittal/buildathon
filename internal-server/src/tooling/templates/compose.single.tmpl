# Auto-generated by Agentic DevOps.
# Single-service compose stack that builds from the local Dockerfile.
services:
  {service_name}:
    build: .
    image: {service_name}:latest
    ports:
      - "{port}:{port}"
    restart: unless-stopped{env_file_block}
