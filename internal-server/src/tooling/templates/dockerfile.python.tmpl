# Auto-generated by Agentic DevOps (stack=python).
# Hand-tune as needed. Safe to re-run scaffolder; it will not overwrite
# this file unless overwrite=True is passed.
FROM python:3.11-slim

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1 \
    PIP_NO_CACHE_DIR=1

WORKDIR /app

# Install system build deps only if needed; keep image small by default.
RUN apt-get update \
 && apt-get install -y --no-install-recommends build-essential \
 && rm -rf /var/lib/apt/lists/*

COPY {dependency_file} ./
RUN {install_cmd}

COPY . .

EXPOSE {port}

CMD {cmd}
