# Auto-generated by Agentic DevOps (stack=node).
# Hand-tune as needed. Safe to re-run scaffolder; it will not overwrite
# this file unless overwrite=True is passed.
FROM node:20-alpine

ENV NODE_ENV=production

WORKDIR /app

COPY package*.json ./
RUN {install_cmd}

COPY . .

EXPOSE {port}

CMD {cmd}
